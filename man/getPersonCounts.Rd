% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getPersonCounts.R
\name{getPersonCounts}
\alias{getPersonCounts}
\title{Get person-count breakdowns and set overlaps for a concept tree}
\usage{
getPersonCounts(
  CDMdbHandler,
  conceptId,
  level = NULL,
  sexStratum = NULL,
  ageStratum = NULL,
  yearStratum = NULL,
  visitStratum = NULL,
  codeCountsTable = "code_counts"
)
}
\arguments{
\item{CDMdbHandler}{A CDMdbHandler object that contains database connection details}

\item{conceptId}{The concept ID to get person counts for}

\item{level}{Maximum tree depth (distance from \code{conceptId}) of concepts to include
in \code{upset_person_counts}. NULL (default) includes the full tree.}

\item{sexStratum}{Optional integer vector of \code{gender_concept_id} values to restrict
\code{upset_person_counts} to. NULL or empty (default) includes all.}

\item{ageStratum}{Optional integer vector of \code{age_decile} values to restrict
\code{upset_person_counts} to. NULL or empty (default) includes all.}

\item{yearStratum}{Optional integer vector of \code{calendar_year} values to restrict
\code{upset_person_counts} to. NULL or empty (default) includes all.}

\item{visitStratum}{Optional integer vector of \code{visit_group_concept_id} values to
restrict \code{upset_person_counts} to. NULL or empty (default) includes all.}

\item{codeCountsTable}{Name of the code counts table in the results schema. Defaults to "code_counts"}
}
\value{
A list containing:
\itemize{
\item \code{filter_person_counts} - Tibble of \code{filter} ("sex"/"age"/"visit"), \code{stratum},
\code{person_counts} — a breakdown of the full tree's persons by each filterable
dimension, independent of \code{level} and the stratum arguments. Lets a client
discover which strata are worth filtering by, and how many persons they hold.
\item \code{upset_person_counts} - Tibble of \code{group} (the concept IDs of the exclusive
region, joined by "-") and \code{person_counts} — exact UpSet exclusive-region counts
for the concepts at or under \code{level}, restricted to the requested strata.
}
}
\description{
Computes exact distinct-person breakdowns for the concept tree rooted at
\code{conceptId}, reading the \code{stratified_persons} person-level bridge table (see
\code{createStratifiedPersonsTable()}). Per-concept \code{person_counts} and
\code{descendant_person_counts} already live in \code{code_counts} and are reachable
via \code{getCodeCounts()}'s \code{concepts} tibble — this function covers what that
table can't answer: stratum breakdowns and exact set-overlap (UpSet) regions.
}
