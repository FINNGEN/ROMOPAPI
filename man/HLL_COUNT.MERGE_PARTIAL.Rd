% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hllCount.R
\name{HLL_COUNT.MERGE_PARTIAL}
\alias{HLL_COUNT.MERGE_PARTIAL}
\title{HLL_COUNT.MERGE_PARTIAL — aggregate HyperLogLog++ sketches.}
\usage{
HLL_COUNT.MERGE_PARTIAL(sketches)
}
\arguments{
\item{sketches}{base64 character vector, OR list of raw vectors, OR a
single raw vector. NA / NULL / empty entries are dropped.}
}
\value{
If input was character: base64 string of the merged sketch.
If input was raw / list of raws: raw vector. \code{NA} if no usable sketches
remain after filtering.
}
\description{
Pure-R port of BigQuery \code{HLL_COUNT.MERGE_PARTIAL}. Drop-in for \code{sum()}
inside \code{dplyr::summarise()} on a column of HLL sketches produced by
\code{HLL_COUNT.INIT} (precision 10–18, encoding_version 2). Returns the
merged sketch (not a cardinality — see \code{HLL_COUNT.EXTRACT} for that).
}
